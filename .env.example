# =============================================================================
# .env.example — Tệp mẫu biến môi trường (KHÔNG chứa giá trị thật)
# Sao chép tệp này thành .env và điền giá trị thực tế:
#   cp .env.example .env
# =============================================================================

# --- SQL Server ---
SA_PASSWORD=BlindBag@SA_2024!
MSSQL_PID=Developer
MSSQL_COLLATION=SQL_Latin1_General_CP1_CI_AS

# --- Port mapping (host port → container port 1433) ---
SQLSERVER_HOST_PORT=1433
